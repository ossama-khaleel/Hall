@model IEnumerable<Hall_Boking_System.Models.Testimonial>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminDashboardLayout.cshtml";
}

@*<p>
    <a asp-action="Create"><i class="fa fa-plus fa-3x" aria-hidden="true"></i></a>
</p>*@
<table class="table table-striped">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TestimonialOpinion)
            </th>
            <th>
                Acceptance
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customers.UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TestimonialOpinion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Acceptlist.AcceptStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customers.UserName)
            </td>
            <td>
                @if (@item.AcceptlistId == 3)
                {
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a> @*|
                <a asp-action="Details" asp-route-id="@item.Id"><i class="fa fa-info" aria-hidden="true"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa fa-ban" aria-hidden="true"></i></a>*@
                }
            </td>
        </tr>
}
    </tbody>
</table>
